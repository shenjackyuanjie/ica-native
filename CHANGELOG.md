# Changelog

本文件记录 ica-native 的变更，遵循 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/) 风格。

## [Unreleased]

### Added
- 屏幕配置新增 `renderer = "glow" | "wgpu"` 渲染后端选项；WGPU 沿用现有 `vsync` 设置，Windows 可通过 `wgpu_backend = "dx12" | "vulkan"` 明确选择图形 API，便于绕过部分 NVIDIA/OpenGL 组合在 VSync 下偶发的交互闪屏。

### Changed
- 同时编译 Glow 与按平台裁剪的 WGPU 渲染后端；旧配置仍默认使用低内存的 Glow，可通过 `[screen] renderer = "wgpu"` 切换并在重启后生效。
- 按测试准则清理测试：删除仅往返验证 TOML 序列化的直通用例，将合并转发「错误响应对象」用例改为直接覆盖回退判定逻辑，并移除历史文件测试中无边界意义的大小断言。

## [0.1.14] - 2026-08-30

### Added
- 窄窗口下可自动在会话列表与聊天内容之间切换；在「定制聊天界面 (ica)」取消「禁用自适应单面板模式」即可启用，聊天页提供返回会话列表的入口。
- 聊天分组侧栏补充当前账号头像入口和「群聊」筛选，与所有聊天、私聊及自定义分组并列展示。
- 群聊标题新增「群文件」入口，可列出目录、创建文件夹、获取下载链接，并将本地文件分块上传到当前群文件目录。
- 群成员面板新增「查看发言记录」，支持按成员分页加载更早消息并忽略过期请求响应。
- 收藏表情支持根目录及一级分类目录，可在选择器中新建分类并将表情移动到目标分类，兼容 Icalingua++ 的 stickers 目录结构。
- 「定制聊天界面 (extra)」新增「关闭聊天分组显示」，可隐藏最左侧分组导航栏；启用时自动回到全部会话，避免保留不可见筛选。

### Changed
- 会话页顶部不再显示 Bridge、认证状态和诊断历史，避免挤占聊天空间；完整诊断继续保留在「选项 → Socket.IO 状态」。
- 「隐藏聊天图片」「隐藏聊天视频」「禁用同会话多图切换」和「禁用图片查看器触摸板手势」设置现已实际影响消息或图片查看器渲染。
- 消息 @ 标记同时兼容旧版 `<IcalinguaAt>` 与新版 XML 转义 `<IcaAt>` 格式；消息时间会根据日期显示为当天时间、同年日期时间或完整日期时间。
- 多选转发在可见消息列表中改用集合判断选中状态，降低长聊天记录下的重复线性查询；成员发言记录采用 20 条分页，避免一次渲染过多历史记录。
- 群成员卡片的昵称、QQ 号与禁言状态改为单行截断；“查看发言记录”和禁言操作合并为同一行。最左侧分组栏标题同步采用固定宽度截断，避免窄栏文字换行撑高布局。

## [0.1.13]

### Added
- 聊天消息在相邻记录跨越本地日期时显示日期分隔，便于区分不同日期的会话内容。
- 合并转发请求、ACK 和解析流程补充调试日志，可定位请求阶段、响应结构、消息数量及具体解析失败位置。
- “关于”窗口新增内嵌更新日志，完整版本历史按版本折叠显示，并默认展开首个版本区块。

### Changed
- 合并转发查看器改为独立系统窗口，保留重新加载和查看内层转发功能；窗口可通过系统关闭操作退出，不响应 Esc。
- 服务端下发的聊天分组不再读写本地配置，编辑结果继续通过 bridge 同步，避免旧配置覆盖服务端状态。
- 界面提示、运行日志和代码注释进一步中文化，并补充 bridge、请求 ID 和失败阶段等诊断上下文。
- 升级 egui/eframe 至 0.36.1，并同步升级 base64、serde_json、roxmltree、toml、tokio、lru 和 linkify 等直接依赖。
- 将 Cargo.lock 中 52 个传递依赖更新到 Rust 1.98 兼容范围内的最新版本；egui、eframe 和 egui_extras 已保持在 0.36.1 的最新 registry 版本。
- 将聊天输入框的 @ 成员选择器、回复预览及快捷键处理拆分为独立模块，降低输入框主模块的维护复杂度。

### Fixed
- 修复聊天界面首次布局时没有正确滚动到最新消息，以及布局稳定前出现位置跳动的问题。
- 修复多行聊天输入框在正文中间按普通 Enter 发送时仍插入换行的问题；Shift+Enter 和 IME 输入行为保持不变。
- 修复回复合并转发或过长消息时，回复预览换行撑高并将输入框挤出预留区域的问题。
- 修复合并转发 ACK 被一层或多层单元素数组包装时误报“缺少或无效的 _id”的问题。
- 修复合并转发调试日志输出消息内容、URL、用户名和资源 ID 等敏感字段的问题，改为仅记录脱敏后的结构摘要。
- 修复右键落在消息正文、链接或 @ 提及时无法打开消息菜单的问题，同时保留文字选择、链接左键打开和图片专属菜单。

## [0.1.12]

### Changed
- 适配 Icalingua 2.26 的 `sendMessage` Socket.IO 协议，图片统一通过 `media[]` 发送；多图消息不再编码成旧的 `raw` 消息负载。

### Fixed
- 修复图片发送仍使用旧 `b64img` 字段导致单图及多图消息无法发送的问题。
- 修复合并转发查看对 2.26 JSON/XML 资源信息解析不完整的问题，支持资源 ID、文件名以及嵌套转发的 fallback 资源 ID。

## [0.1.11]

### Added
- 聊天列表新增蓝色草稿标记；会话存在非空草稿时优先预览草稿内容，便于快速定位尚未发送的消息。
- 消息正文、回复及合并转发窗口支持高亮显示 @ 成员，并在悬停时显示对应 QQ 号；QQ 表情与 @ 标记可在同一段内容中正确混排。
- 聊天消息正文及引用回复支持鼠标划词选择；HTTP(S) 链接和无协议域名会自动高亮，点击后使用系统浏览器打开，并避免将相邻中文标点及正文误识别为链接。
- 外观设置新增高对比度聊天界面选项，在深色模式下提高聊天正文和合并转发内容的可读性。
- 群聊消息发送者名称旁新增群主、管理员及群头衔显示；普通成员和 Telegram 桥接消息不会显示 QQ 群身份信息。
- 顶部错误和通知新增清除按钮，并为每个 Bridge 提供带时间戳的最近消息历史下拉框及清空操作。

### Changed
- 群聊 @ 成员选择器改为锚定输入框上方的悬浮面板，不再占用消息列表高度；候选项支持搜索、鼠标悬停高亮、方向键循环选择、Enter 确认、Esc 或点击外部关闭，并新增 `Ctrl+M` 快捷入口。
- @ 成员选择状态在打开、关闭、发送、切换会话及切换 Bridge 时统一重置，选择或主动关闭后恢复消息输入焦点。
- 合并转发控制标记改为可读的摘要预览和查看入口，支持解析与 ica 本体一致的 JSON `source/news` 及 XML `title` 文本。

### Fixed
- 修复聊天消息时间直接按 UTC 显示的问题，改为根据系统本地时区显示，确保中国时区下显示为 UTC+8。
- 修复搜索消息响应 ACK 多一层数组导致搜索结果无法解析的问题，并统一复用消息列表响应的数组规范化逻辑。
- 修复收藏表情面板复用普通表情的图标尺寸和列数计算，导致收藏表情布局异常的问题。
- 修复被引用的消息包含图片时引用卡片不显示图片的问题；普通聊天布局显示缩略图，纯文本布局显示图片提示。
- 修复 @ 成员面板打开时按 Esc 会继续退出当前会话，以及窄窗口下弹层定位边界可能无效的问题。
- 修复粘贴图片或插入 @ 成员后输入区高度变化时，消息列表自动收缩导致输入框上移，或消息区最小高度将输入框挤出窗口底边的问题。
- 修复按钮在悬停、按下或打开状态下绘制边界外扩，造成相邻控件视觉跳动的问题。
- 修复 ica 窗口可见但未聚焦时，在其他应用按下 Ctrl+V 仍会触发 ica 粘贴剪贴板图片的问题。

## [0.1.9]

### Added
- 新增联系人窗口，可分别加载、搜索好友和群联系人，并从尚未出现在会话列表中的联系人直接创建和打开聊天。
- 新增合并转发查看功能：识别普通与嵌套合并转发消息，支持加载远端转发记录、查看内层转发、图片、附件和回复内容，并显示请求失败或超时状态。
- 消息多选栏新增合并转发模式；转发目标支持同时勾选多个群聊或私聊，并可搜索、全选当前结果、清空目标以及切换合并转发/逐条转发。

### Changed
- Bridge 新增类型化联系人、合并转发读取和合并转发发送命令及响应事件；读取请求增加 15 秒超时和过期响应保护。
- 合并转发节点优先复用原始消息链，缺失时回退构造回复、文本、QQ 表情、图片和语音提示，并根据来源和目标会话自动选择群聊或私聊参数。

### Fixed
- 修复 Bridge 毫秒时间戳被按微秒解析的问题，避免消息显示时间及合并转发节点时间错误。

## [0.1.8]

### Added
- 群聊标题新增群成员入口和可调整宽度的右侧管理面板，支持成员搜索、全部/禁言筛选、角色与剩余禁言时间展示，以及带确认的预设/自定义禁言和解除禁言。

### Changed
- 群禁言改用类型化 `SetGroupBan` 命令；无 ACK 的 Bridge 请求只提示“请求已发送”，约 1 秒后自动刷新并保留旧成员列表。`rust-socketio` 改为固定公开 fork revision，删除仓库内 vendor 副本。

### Fixed
- 群成员 ACK 增加独立 15 秒超时守卫，ACK 与超时通过原子完成标记竞争，避免断线时加载状态永久卡住或重复处理迟到响应。

## [0.1.7]

### Added
- 消息图片和图片查看器新增复制原图、复制 URL、打开、保存、另存为及定位原消息等操作；图片解码、系统剪贴板和文件写入改由后台媒体任务执行，并在界面中反馈成功或失败。
- 新增基础收藏表情功能：兼容 Icalingua++ 的共享 `stickers` 目录，路径不可用时回退到 ica-native 数据目录；收藏前校验图片并通过临时文件原子写入，损坏文件不会阻塞其他表情加载。
- Composer 表情面板新增“QQ 表情 / 收藏表情”页签，收藏表情可作为待发送图片加入当前会话。

### Changed
- 重构应用为运行时、配置和状态组成的壳层，并按聊天、设置、聊天分组、收藏表情、自动签到、关系网及管理工具等功能拆分模块。
- 将全局配置改为可注入的 `ConfigStore`，保持既有 TOML 字段和默认值兼容，并统一应用启动、图片加载器配置及退出保存流程。
- Bridge 通信改用类型化 `BridgeEvent` 和 `IcaCommand`，未知协议事件仍保留原始名称与负载；连接句柄、状态和停止通道合并为独立会话，房间消息、草稿、回复、附件及滚动状态统一收敛到会话状态。
- 将关系网拆分为模型、状态、布局、控制器和视图，布局逻辑不再依赖界面实现。

## [0.1.6]

### Added
- 新增关系网斥力强度、好友边长、群边长和群成员边长配置，可通过 `ui_setting.relation_network` 调整力导向布局。

### Changed
- 力导向布局扩展到总览、聚焦和多选关系视图，并以 24ms 的真实时间间隔持续推进，不再受固定 step 数限制，也不会因额外重绘突然快进。
- 力导向斥力改为通过空间网格查找真实近邻，并将聚焦节点或总览中的“自己”节点固定在画布中心；群成员分批加载期间暂停布局动画，避免反复重建造成界面无响应。
- 默认提高关系网斥力，并按关系类型使用不同弹簧长度，使好友、群和群成员形成更清晰的分层与局部簇。
- 扩大力导向坐标和弹簧长度范围，为群与好友使用独立的径向层级；同类节点按稳定哈希使用不同边长，并随可见节点数扩大径向范围，避免大量节点堆在同一条外圈边界上。
- 将好友、群、群成员和聚焦视图的初始位置、关系边、斥力范围及运动速度统一放大 10 倍；群节点按各自的弹簧目标半径初始化，布局活动边界同步扩展，避免首帧或收敛后挤成一圈。
- 群节点颜色根据已加载成员数使用由亮红到深红的对数渐变，人数越多颜色越深；成员数未知时保留基础群颜色。
- 群成员加载进度统一保留在侧边栏，移除重复的顶部进度条，并改用实时群列表计算完成比例。

### Fixed
- 修复独立关系网窗口未随后台成员响应刷新进度、统计和图谱，以及加载首帧仍使用动作执行前旧快照的问题。
- 修复完全重叠的节点无法产生有效斥力，以及加载成员期间持续启动布局动画导致窗口无响应的问题。

## [0.1.5]

### Added
- QQ 关系网新增多选模式，可选择多个节点后查看它们之间的共同关系网。
- QQ 关系网新增悬浮节点详情卡片，展示 QQ/群号、成员数、共同群、关联数和角色信息。
- QQ 关系网新增性能等级徽标、当前视图提示、统计卡片和节点大小图例。
- QQ 关系网聚焦视图新增带预热和逐帧收敛的轻量力导向布局：以弹簧力、抽样斥力、中心弱引力和速度阻尼组成，聚焦节点固定在原点，布局确定性可复现，大图采用固定上限的邻居抽样将复杂度控制在近似 `O(节点数 + 边数)`。
- 新增「切换会话时自动拉取历史消息」开关，开启后从协议端请求最新漫游记录（新增 `IcaCommand::FetchLatestHistory` 与 `fetchHistory` 协议实现，沿用 Icalingua++ 的 17/21 字节占位消息 ID 规则），关闭时只读取 bridge 缓存。

### Changed
- 重构 QQ 关系网侧边栏，使统计、筛选、加载、聚焦和多选操作更接近 Web 版工具体验。
- 优化 QQ 关系网画布样式，复刻 Web 版节点配色、好友/群白色描边、多选淡化和连线透明度分级。
- 改进一级关系网和多选关系网的节点过滤逻辑，使 native 版更贴近 `qq-relation-network` 的关系展示规则。
- 重写 QQ 关系网窗口为顶栏、卡片侧栏和独立画布布局，并加入缩放、平移和按群关系聚类的拓扑布局。
- 缓存关系网可见节点、连线和布局结果，并为节点与群列表建立索引，降低大图稳定重绘时的 CPU 与分配开销。
- QQ 关系网改为跟随应用明暗主题，并将数据模型、布局缓存、主题和界面拆分为独立模块。
- QQ 关系网颜色直接派生自应用当前 egui 视觉设置，避免关系网窗口与主窗口出现不同色调。
- 群成员加载改为限流并发、持续补位和分批图谱重建，增加实时完成/加载中/等待中进度以及构建耗时日志。
- 大量好友与群并存时使用分区径向布局，并降低密集视图的描边和连线噪声。
- 独立关系网窗口的加载/刷新动作改为主动唤醒父视口（`request_repaint_of`），修复 deferred 子视口点击后主视口不消费动作、必须关闭窗口才开始加载的问题。
- 后台 bridge 事件改为经入口队列转发并主动 `request_repaint` 根视口，使聊天消息、群成员和连接状态等事件不依赖鼠标操作或关闭子窗口即可及时推进关系图重建。

### Fixed
- 修复 QQ 关系网按钮悬停时绘制范围外扩造成的尺寸跳动。
- 修复关系图随窗口宽高比变化发生非等比拉伸的问题。
- 修复多选模式尚未选择节点时错误淡化全部节点的问题。
- 修复 `rust-socketio` 随机 ACK ID 冲突导致回调串线和 `Vec::remove` 越界 panic 的问题。
- 修复独立关系网窗口中加载动作无法及时触发的问题。
- 修复进入会话后的滚动偏移及零散消息阻止完整历史加载的问题：区分「已请求完整快照」与零散到达的 `addMessage`、避免首帧 offset 误判为滚到顶部而误触发旧消息加载、底部锚点保留到可见消息高度稳定、并在 `commandFailed` 后清理请求占位以允许重试。

## [0.1.4]

### Added
- 支持通过配置调整 QQ 关系网渲染节点数、连线数、标签数和自动降级阈值。

## [0.1.3]

### Added
- 新增「QQ 关系网」原生窗口，可从顶部「选项」菜单打开。
- 支持基于现有好友会话、群会话和群成员缓存构建关系网节点与边。
- 支持按节点类型筛选自己、好友、共同群好友、仅同群和群节点，并显示节点数量统计。
- 支持批量加载群成员、重建关系网、搜索昵称/QQ/群号，以及点击节点查看一跳关系和节点详情。

### Changed
- 关系网随 `onlineData`、`setAllRooms` 和 `groupMembersResponse` 自动重建，并在大图场景下自动关闭部分高成本显示项。
- 使用轻量原生径向布局渲染关系图，避免引入 Flask/ECharts/NapCat 侧服务依赖。

## [0.1.2]

### Changed
- 降低默认图片内存缓存预算到 128MiB，减少图片密集会话下的常驻内存。
- 表情 APNG 改为按需从 `assets/face` 读取，并使用小型 LRU 缓存，避免启动时常驻全部表情字节。
- GIF 预览增加帧数和解码后字节预算，避免长动图一次性解码所有帧造成内存峰值。
- 聊天消息增加渲染用时间文本缓存，减少消息列表重绘时的重复格式化。
- 会话列表过滤/排序结果增加 revision 缓存，减少每帧重复分配、过滤和排序。

### Fixed
- 限制房间消息、搜索结果和消息布局缓存增长，切换会话时清理非活跃布局缓存。
- 裁剪历史消息缓存时区分新消息追加和旧历史 prepend，避免加载旧历史后错误丢弃刚拉到的旧消息。
- 原始消息链只在可用于原样转发时保留，降低历史消息结构的重复 JSON 占用。

## [0.1.1]

### Added
- 新增「搜索聊天记录」功能
  - 在会话列表右键菜单、会话顶部工具栏均可打开搜索窗口
  - 通过 `IcaCommand::SearchMessages` 向 bridge 发起 `searchMessages` 请求，并解析 `searchMessagesResponse`
  - 支持分页加载更多结果（每页 20 条），结果去重后追加
  - 搜索结果支持回复、撤回、转发、预览图片、跳转原消息等已有消息操作
  - 新增 `MessageSearchState` 管理搜索窗口状态，新增 `message_search` 模块与对应渲染窗口

### Changed
- 将 Icalingua 兼容协议版本号 `ICA_PROTOCOL_VERSION` 从 `2.12.28` 更新到 `2.26.0`，
  以匹配 Icalingua-plus-plus 最新要求的 `EXCEPTED_PROTOCOL_VERSION`
- 将 `is_image_file_type` 可见性从 `pub(super)` 提升为 `pub(crate)`，供搜索结果图片预览复用

### Fixed
- 搜索失败时通过 `commandFailed` / `searchMessages` 错误事件回写错误信息，避免静默失败
